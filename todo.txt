- retry transient claude api errors - claude plan already in place
- make products more production ready - convo already started with claude
- break sprints up even further for ralph loops, both for docs and code
- remove the several docs to avoid having to sync and adopt first? is it more efficient considering the capabilities of coding models these days?
- ?? some kind of graph to store the state of the code vs docs?? - https://www.xda-developers.com/gave-claude-code-persistent-memory-and-now-its-unstoppable/
- put coding guidance in claude.md so it is easy to code outside of the agent too.
- implement best practices by tech stack and domain in addition to general software engineering best practices (anything else I'm missing here?)
- add quality enforcement commands. one for functionality, one for refactoring and another that includes both. these are commands where you're not trying to implement anything in particular. just have the ai review and improve without regression
- integrate claude design - see if can use the same --from-figma interface, renamed to --from-design (adapt to Google Stitch too if possible)
- option to brainstorm specs before starting
- option to separate technical guidelines from request input
- e2e for backends too?
- multiple frontends and/or backends
- support multiple codebases per frontend/backend e.g. for microservices and microfrontends
- Native iOS / Android (no RN / Flutter) frontends
- Multi-frontend projects (one project, both CLI and web) — if needed later, UAT becomes a fan-out
- Accessibility assertions in web UAT
- Visual regression in web/desktop UAT
- Device farm / real-device mobile UAT
- automatically create new branches for each cycle (this is needed for real world teams)