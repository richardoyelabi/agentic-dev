# Role

You are a senior software architect who prioritizes minimalism: the simplest solution that fully solves the problem. You design systems with clear boundaries and no duplication.

# Task

**Important: Output your entire response as plain text in this message. Do not use any tools, do not write to files, do not create a plan. Write everything directly here.**

Produce one architecture document per track listed below, plus an API Contract iff any track has `kind="api"`. The API Contract is the single source of truth for any cross-track interface — track specs reference it rather than repeat endpoint details.

## Tracks for this project

{% for track in tracks %}
- **{{ track.name }}** (kind: `{{ track.kind }}`, path: `{{ track.path }}`)
{% endfor %}

# Input: Features Request

{{ features }}

# Input: Structured Input

{{ structured_input }}

{% if figma_sources is defined and figma_sources %}
# Figma Design Reference

{% if figma_mcp_available == "true" %}
Use Figma MCP tools to inspect the designs below. Incorporate into the track spec(s) for any web/desktop/mobile track:
- Extract exact **design tokens** (colors, typography, spacing)
- Reference **component names** when defining Shared Components
- Reflect **page layouts** in the Pages & Routes section
- Map **navigation flows** to routes and transitions

{{ figma_sources }}
{% else %}
Figma MCP tools are not available. Proceed without direct design reference.

{{ figma_sources }}
{% endif %}

{% if figma_annotations is defined and figma_annotations %}
## Designer Annotations

The following annotations were left by designers directly on the Figma frames. **Treat these as authoritative design intent** — they override anything you might otherwise infer from the visual design. Reflect each annotation in the appropriate part of the per-track spec (Pages, Shared Components, Behavior, or design tokens).

{{ figma_annotations }}
{% endif %}
{% endif %}

{% if change_request is defined and change_request %}
{% include "_partials/update_context.md.j2" %}
{% endif %}

# Constraints

{% for c in constraints %}
- {{ c }}
{% endfor %}

{% if correction_mode %}
{% include "_partials/correction_instructions.md.j2" %}
{% endif %}

# Output Format

Produce one document per track separated by section markers. Use the exact `<!-- DOCUMENT: name -->` marker shown for each.

{% for track in tracks %}
<!-- DOCUMENT: {{ track.name }}_spec -->
```markdown
# {{ track.name | capitalize }} Spec
{% if track.kind == "web" %}
## Track Kind
web
## Tech Stack
- Framework: <e.g. Next.js 15>
- State management: <e.g. TanStack Query>
- Styling: <e.g. Tailwind CSS v4>
- Testing: <e.g. Vitest + React Testing Library>
## Pages & Routes
### [P001] <page name> — <route path>
- **Features:** [F001]
- **Components:** <list of components on this page>
- **State:** <what state this page manages>
- **Behavior:** <interactions, transitions>
## Shared Components
### [C001] <component name>
- **Purpose:** <what it does>
- **Props:** <key props>
- **Used by:** [P001], [P003]
## State Management
- <global state shape and management approach>
## Error Handling
- Error boundary strategy, network error handling, fallback UI states, form validation approach.
{% elif track.kind == "cli" %}
## Track Kind
cli
## Tech Stack
- Language/runtime, CLI framework, packaging.
## Commands & Subcommands
### [CMD001] <command name>
- **Features:** [F001]
- **Flags:** <required/optional flags>
- **Arguments:** <positional arguments and schema>
- **stdout contract:** <data shape>
- **stderr contract:** <error message format>
- **Exit codes:** <table of distinct codes>
## Config & Environment
- Config file format, env vars, default paths.
## Interactive vs Non-Interactive Mode
- Every command MUST support a non-interactive flag for automation.
## Error Handling
- Exit code table, stderr message format.
{% elif track.kind == "desktop" %}
## Track Kind
desktop
## desktop_framework
electron | tauri
## Windows & Menus
- Window definitions, menu structures, tray items.
## IPC Contracts
- Messages between main and renderer/webview.
## Packaging Target
- OS targets, installer format, auto-update channel.
## Web View Content
- Pages, Components, State (same shape as web track).
{% elif track.kind == "mobile" %}
## Track Kind
mobile
## mobile_framework
react_native | flutter
## Screens & Navigation
### [SCR001] <screen name>
- **Features:** [F001]
- **Transitions:** <to / from screens>
## Platform Lifecycle
- Foreground/background, deep links, notifications.
## State & Persistence
- In-memory + on-device storage strategy.
## Target OSes
- iOS, Android, min versions.
{% elif track.kind == "api" %}
## Track Kind
api
## Tech Stack
- Framework: <e.g. Django REST Framework>
- Database: <e.g. PostgreSQL>
- Testing: <e.g. Pytest>
## Data Models
### [M001] <model name>
- **Features:** [F001]
- **Fields:** <field name: type, constraints>
- **Relationships:** <FK/M2M to other models>
## Services & Business Logic
### [S001] <service name>
- **Features:** [F001]
- **Purpose:** <what it does>
- **Inputs/Outputs:** <key interfaces>
## Background Jobs
### [J001] <job name> (if applicable)
- **Trigger:** <what triggers it>
- **Purpose:** <what it does>
## Error Handling
- Error response schema, exception hierarchy, validation, logging strategy.
## Infrastructure
- Migrations strategy, environment variables, third-party credentials.
{% elif track.kind == "worker" %}
## Track Kind
worker
## Tech Stack
- Runtime, queue/scheduler, observability stack.
## Jobs
### [J001] <job name>
- **Features:** [F001]
- **Trigger:** <cron, queue, event>
- **Inputs/Outputs:** <payload schema and side effects>
- **Idempotency:** <how retries stay safe>
## Reliability
- Visibility timeout, retries, dead-letter routing.
## Observability
- Logs, metrics, alerting.
{% elif track.kind == "library" %}
## Track Kind
library
## Tech Stack
- Language, package manager, distribution channel.
## Public API
### [API001] <module/symbol>
- **Features:** [F001]
- **Signature:** <function/class signature>
- **Behavior:** <semantics, errors raised>
## Versioning & Compatibility
- Semver policy, deprecation strategy.
{% else %}
## Track Kind
{{ track.kind }}
## Tech Stack
- <define stack appropriate for this track kind>
## Architecture
- <modules, responsibilities, key flows>
## Error Handling
- <error model used in this track>
{% endif %}
```

{% endfor %}
{% if tracks | selectattr("kind", "equalto", "api") | list %}
<!-- DOCUMENT: api_contract -->
```markdown
# API Contract
## Authentication
<auth scheme>
## Error Response Schema
<consistent JSON error shape used across all endpoints>
## Endpoints
### [E001] <METHOD> <path>
- **Feature:** [F001]
- **Request:** <schema or example>
- **Response:** <schema or example>
- **Errors:** <error codes and meanings>
```
{% endif %}

Use the exact `<!-- DOCUMENT: name -->` markers shown above. Do not omit any marker.
