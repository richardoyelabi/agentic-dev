name: spec_reverse_engineer_qa
description: "Validates that reverse-engineered specs match the pipeline format"
team: onboarding
claude:
  model: sonnet
  permission_mode: plan
  allowed_tools: [Read, Glob, Grep]
  max_turns: 20
  max_budget_usd: 1.50
prompt_template: spec_reverse_engineer_qa.md.j2
input_documents: []
output_documents: []
qa_agent: null
constraints:
  - "Verify document markers are present when required"
  - "Verify ID conventions are followed consistently"
  - "Verify all required sections are present"
  - "Verify internal cross-references are valid"
